# GPU Matmul Lab

**Status:** Planned (Milestone 4)

Compare matrix multiplication across: Python loops, NumPy, PyTorch CPU, PyTorch GPU,
and different numeric precisions. Measure latency, throughput, and memory.
