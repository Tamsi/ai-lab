# Latent World Model

**Status:** Planned (Milestone 6)

Simple 2D environment: current state + action → dynamics model → predicted future state.
Start with vector states; optionally add a visual encoder and latent space later.
