# Minimal SFT Loop

**Status:** Not started

## Objective

Fine-tune tiny LM on (prompt, response) pairs with masked loss

## Constraints

- Implement core logic in `solution.py`
- Tests in `test_solution.py` must pass after you remove `pytest.mark.skip`

## Tasks

1. Read concept note: `notes/llms/supervised-fine-tuning.md`
2. Implement required functions
3. Run `python scripts/submit.py pytorch/sft_mini`
4. Document in `results.md`

## Reflection

- What surprised you?
- What links to papers you are reading?
